import 'package:flutter/material.dart';

class FileTaxInitialTaxAdviceScreen extends StatelessWidget {
  const FileTaxInitialTaxAdviceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}